/*
  Public browser configuration only. The Supabase publishable key is designed
  to be included in frontend code. Never put a secret key or service-role key
  here. The app remains fully local when these values are blank.
*/
window.FINANCE_CLOUD_CONFIG = Object.freeze({
  supabaseUrl: "",
  publishableKey: ""
});
